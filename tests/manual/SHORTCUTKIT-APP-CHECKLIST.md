# ShortcutKit v0.2.0 实机验收

日期：待执行  
测试提交：待填写  
测试机器：macOS（当前用户机器）

## 自动门禁

- [ ] Swift 单元测试与 App 自检
- [ ] Lua 模块测试
- [ ] Shell 生命周期测试
- [ ] App 打包与公开文件审计
- [ ] DMG、ZIP 和 SHA-256 校验

## App 与配置

- [ ] 从打包后的 `ShortcutKit.app` 启动，菜单栏图标和设置窗口可见
- [ ] 12 个模块、普通按键编辑器和特殊手势标签完整显示
- [ ] 关闭/开启 `window_screenshot` 后，JSON 与 Hammerspoon 状态一致
- [ ] 关闭/开启 `codex_toggle` 后，JSON 与 Hammerspoon 状态一致
- [ ] 把窗口截图从 `⌘ R` 临时改为非冲突组合，连续截图 3 次成功
- [ ] 单项恢复 `⌘ R`，连续截图 3 次成功
- [ ] 内部重复组合被阻止并显示冲突动作
- [ ] 可报告的 macOS 系统组合被阻止并显示系统冲突
- [ ] 全部恢复默认按键不改变模块开关，也不删除未知未来字段

## 兼容与恢复

- [ ] OCR、Chrome、邮箱大师、ChatGPT Classic、WhatsApp 精确范围按当前依赖状态验收
- [ ] 长按右 Option 与左键+C/V/D 保持原行为
- [ ] 普通点击、拖拽和三指拖移保持透传
- [ ] App“安装或修复”、CLI 更新、卸载、恢复、最终重装通过
- [ ] 最终只有一个加载块，12 个模块无运行错误

## 结果

待实机执行后填写；无法自动验证的物理手势不得标成已通过。

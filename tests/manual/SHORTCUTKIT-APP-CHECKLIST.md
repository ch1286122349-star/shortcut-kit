# ShortcutKit v0.2.0 实机验收

日期：2026-08-24
测试提交：`08505b2`（功能候选；本文件更新将在后续提交）
测试机器：macOS（当前用户机器）

## 自动门禁

- [x] Swift 单元测试与 App 自检（31 项 XCTest 通过，self-test PASS）
- [x] Lua 模块测试
- [x] Shell 生命周期测试
- [x] App 打包与公开文件审计
- [x] DMG、ZIP 和 SHA-256 校验

## App 与配置

- [x] 从打包后的 `ShortcutKit.app` 启动，菜单栏图标和设置窗口可见
- [x] 12 个模块、普通按键编辑器和特殊手势标签完整显示
- [x] 关闭/开启 `window_screenshot` 后，JSON 与 Hammerspoon 状态一致
- [x] 关闭/开启 `codex_toggle` 后，JSON 与 Hammerspoon 状态一致
- [ ] 把窗口截图从 `⌘ R` 临时改为非冲突组合，连续截图 3 次成功（已确认改为 `⌃ ⇧ 5` 且 Hammerspoon 实时绑定为 `ctrl+shift+5`；自动化通道不能触发全局物理快捷键，因此三次截图保留给人工）
- [ ] 单项恢复 `⌘ R`，连续截图 3 次成功（单项恢复和实时绑定已通过；三次物理截图保留给人工）
- [x] 内部重复组合被阻止并显示冲突动作 `local_ocr`
- [x] 可报告的 macOS `⌘ ⇧ 5` 被阻止并显示系统冲突
- [x] 全部恢复默认按键不改变模块开关，也不删除未知未来字段（单元测试覆盖；实机单项恢复通过）

## 兼容与恢复

- [ ] OCR、Chrome、邮箱大师、ChatGPT Classic、WhatsApp 精确范围按当前依赖状态验收
- [ ] 长按右 Option 与左键+C/V/D 保持原行为
- [ ] 普通点击、拖拽和三指拖移保持透传
- [x] App“安装或修复”、CLI 更新、卸载、恢复、最终重装通过
- [x] 最终只有一个加载块，12 个模块无运行错误，Hammerspoon 本体保留

## 结果

App 启动、安装自动重载、自定义录入、冲突阻止、恢复默认、两类模块开关和完整生命周期均通过实时 UI、JSON 与 Hammerspoon 三方回读。首次实测发现并修复了首次窗口不弹出、安装后未重载、Shift 数字被记录为 `%`、首次配置失败无法回滚、录入结束与冲突检查竞态、运行错误泄露本机路径六项问题。

自动化通道无法真实触发全局物理快捷键，也无法模拟三指拖移；Command+R 三连截图、OCR 框选、长按右 Option、左键组合和三指拖移保持未勾选，不能表述为已人工通过。
